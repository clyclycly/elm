border-1px($color)
  position relative
  &:after
    content ' '
    display block
    position absolute
    bottom 0
    left 0
    width 100%
    border-bottom 1px solid $color

border-none()
  &:after
    display none
/*现在在ios7以下，android等其他系统里，0.5px会被显示为0px，这个方案不完美4个边框要写四个这玩意，用rem*/
@media (-webkit-min-device-pixel-ratio: 1.5),(min-device-pixel-ratio: 1.5) //选中dpi比1.5大。(min-device-pixel-ratio: 1.5)不是webkit的时候
  .border-1px
    &:after
      -webkit-transform scaleY(0.7)
      transform scaleY(0.7)

@media (-webkit-min-device-pixel-ratio: 2),(min-device-pixel-ratio: 2) //选中dpi比2大。(min-device-pixel-ratio: 1.5)不是webkit的时候
  .border-1px
    &:after
      -webkit-transform scaleY(0.5)
      transform scaleY(0.5)

bg-image($url)
  background-image url($url + "@2x.png")
  @media (-webkit-min-device-pixel-ratio: 3),(min-device-pixel-ratio: 3) // 苹果6pdpi为3
    background-image url($url + "@3x.png")
